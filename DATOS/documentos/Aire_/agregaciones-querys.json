db.aire.aggregate(
   [
      {
        $group : {
           _id : "$Provincia",
           count: { $sum: 1 }
        }
      },
	  { $sort : { count : -1 } }
   ]
);

db.aire.aggregate(
   [
      {
        $group : {
           _id : "$Ciudad",
           count: { $sum: 1 }
        }
      },
	  { $sort : { count : -1 } }
   ]
);

db.twitter.find({$and:[{place:{$ne:null}},{place:{$exists:true}}]}).pretty();